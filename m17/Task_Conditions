Обратная польская запись
Необходимо вычислить выражение написанное в обратной польской записи (ПОЛИЗ). На вход подается строка состоящая из целых чисел и арифметических символов (+, -, *, /). Знак минус возможен как в бинарном, так и в унарном варианте, в последнем случае он обозначается как @. В ответ единственное целое число - результат. Для решения задачи необходимо использовать стек. Можно написать свой или воспользоваться стеком из STL.
Input format
Строка состоящая из целых чисел и символов '+', '-', '*', '/', '@', ' '. В конце строки символ '\n'. Строка корректная. Длинна строки не превосходит 1000 символов. Все числа и операции разделены ровно одним пробелом.
Output format
Целое число, результат вычисления выражения.
Examples
Input
3 4 + @
Output
-7
Input
1 2 + 4 * 3 +
Output
15
